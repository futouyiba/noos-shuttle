# docs/ 文档治理（v0）——提案

> Authority-Level: Candidate
> Owns: DocGovernance
>
> 提案，不是契约。待 Epic Designer 裁定；裁定前不实施。
> 任务 issue：`noos-shuttle#76`。参考模型：中鱼升级项目 Notion 文档治理（经人类指定）。
> **本提案不改变任何既有文档的内容与结构**；它提议的是一套规则，落地另行切片。
>
> 本版为回应独立复审 `REQUEST_CHANGES` 的修订版；逐条处置见**附录 C**。

## 0. 一句话

**规范早就有「权威文件」这个概念（§1.4），缺的只是「怎么声明」。**
本提案补的就是这个声明形式，并补上仓库文档层缺的那一半——一个入口，和负面知识的统一归属。

## 1. 问题

### 1.1 现状盘点（先看已有的，避免把已解决的问题当问题）

**已经有的**（实证，非推测）：

| 已有实践 | 证据 |
| --- | --- |
| **被取代文档的声明已基本一致** | v0 / v1 / v2 / `reducer-operations-v0` / `n1-n5-amendment-v0` / `m1-m3-amendment-v0` 六份**前两行逐字节相同**：<br>`> **SUPERSEDED — do not use as current design input.**`<br>``> Current candidate: `cross-agent-handoff-escalation-contract-v3.md` (consolidated current contract)``<br>（**第三行有微差**：v1 为 `Full historical v1 re-review target is frozen at exact commit:`，其余五份为 `... content is frozen at ...`；且 v1 缺 `provenance pointer only` 一行。见**第一轮 n1**） |
| **被吸收层的 tombstone 清单** | v3 头部：「Supersedes as the current reading path（semantics fully absorbed herein; files retained as provenance tombstones）」+ 四份文件清单 |
| **裁定记录（形态雏形）** | `v1-adjudication-record.md`：注明裁定来源、结论、`treated as final`、authority baseline 新 SHA |
| **冻结证据** | `docs/deliberation-harness/evidence/*.json`（**4 份**） |
| **提案不得删除** | 规范 §2.1：「proposal 文档一经裁定引用即不得删除；如需清理，先以 commit SHA 引用固化」 |

**带权威类头部的文档：7 份**（六份 `SUPERSEDED` + v3）。

> 计数更正：初稿写「8 份」。第 8 个命中是 `implementation-slice-1-submission-ledger-report.md`，
> 它命中的是**正文散文**里的「…was superseded and is not on `main`」，**并非权威类头部**。
> 感谢复审指出。

### 1.2 真正缺的四件事

上面那套实践很好，但它是**从「被取代方」写的**。缺的是另一半：

**缺口 1 —— 没有「我是 Current」的正面声明。**
六份文档都指向 v3，但 **v3 自己只说 `Status: Consolidated Current Design Contract`**；
`v1-primary-design-current.md` 标题含 "Current"，却自己声明「authoritative mechanics remain in the contracts」——
**它不声称自己是 mechanics 的权威**。

结果：**「概念 X 的权威是哪份」仍然必须靠读、靠猜。**

**缺口 2 —— 没有所有权声明。**
没有任何文档说「我拥有概念 X」。两份文档都声称权威时，**无法机械检出冲突，也无从裁决**。

**缺口 3 —— 没有入口。**
`docs/` 下**不含本文**有 **61 份 `.md` / 12,640 行**，另加 **4 份 JSON 证据**，共 **65 个文件**。
**没有 README。** agent 必须已经知道目录结构才找得到东西，且无法知道自己是否读全了。

> 计数口径（依**第一轮 m1** 与**第二轮 NIT-1** 要求）：以上为**不含本文**的稳定口径——**本文刻意不引用包含自身的行数**，
> 因为任何编辑都会让那个数字立即过期（本项在两轮复审中错了两次，故直接去掉该自指数字）。
> 两点注意：`find docs -type f` 会把 4 份 JSON 计入，故「文件数」与「`.md` 份数」相差 4；
> 全部 65 个文件合计 **23,152 行**（含 JSON），与 `.md` 口径的 12,640 行不可混用。
> 以上**四个**数字（61 / 65 / 12,640 / 23,152）**均为不含本文的稳定口径**。

**缺口 4 —— 负面知识无统一归属。**

> **本节经两轮更正，且第二次是复审否证了第一次的更正。**
> 初稿写「**没有任何容器**」——**假的**；
> 本文自查后改称「**已有 2 份文档**带 `## Rejected Alternatives` 章节」——**也是假的**（复审实测：
> 其中 `noos-context-pack-background-layer.md` 的那处在**代码围栏内**，是 §5.6 的模板**建议**而非真实章节；
> 另一份的标题是小写 `## Rejected alternatives`，与所引不逐字节）。
> **两次都错在同一件事上：先写结论，再去数。**

**准确的命题**：负面知识**已有零散容器，但形态各异、无统一归属**。举例（**不穷举**）：

| 文档 | 该容器叫什么 |
| --- | --- |
| `deliberation-harness/v1-primary-design-companion-decision-memory-v0.md` | `## Rejected alternatives`（含 Reopen condition 列） |
| `noos-llm-wiki-bridge.md` | `## Why Not Reuse One Directory Directly` |
| `noos-hub-local-write-channel.md` | `### Why Not Native Messaging First` |
| 多份 harness 文档 | `## Non-goals` / `## What this slice deliberately does not do` |
| `noos-hub-shuttle-user-guide.zh-CN.md` | `### 为什么不能一键装进日常 Chrome？` |

**同一类知识，命名各异、散落在不同文档的正文里**——与下面的**附带缺口（裁定记录）完全同形**：
实践已存在，缺的是**统一形态、编号与入口**。

> **写成举例而非计数，是本节的第三次修订，也是唯一稳的那个写法**：
> 只要写成「共 N 份」或「没有任何」，就会在下一次有人新增一个容器或有人查出我漏掉一个时变成假命题。
> 这与本文档反复出现的失败模式是同一个根：**把可变的计数写进正文**。

**旁证（为什么标准化仍然必要）**：designer 反复在裁定里写
*"Do not reopen PR #57 / Issue #56 Q1–Q6"*——即**「逐条点名某个 PR / Issue 的 Qn，宣布不得重开」**
这一类裁定，目前只存在于评论里，不在任何可被入口检索的位置。
（**窄读**：这里说的是那类**点名裁定**，不是说「负面知识一律不在文档里」——
上面那张表已经证明后者为假。）

**附带缺口 —— 裁定记录无统一形态、无编号、无入口。**

> **本节经三轮复审三次更正。** 初稿写「两份 proposal 正文命中 0」；第二版改为「默认分支无任何裁定条目」
> ——**两者都不准确**；第三版列了「三种形态」，**漏了第四处**。以下为实测。

**本仓已有四种裁定记录位置**（实测，其中两处在默认分支上）：

| # | 形态 | 位置 | 是否在 `main` | 完整度 |
| --- | --- | --- | --- | --- |
| 1 | 转述式记录 | `docs/deliberation-harness/v1-adjudication-record.md` | **是** | 记来源与结论，**无逐字引用块** |
| 2 | **逐字存档** | **`docs/carrier-summon-v2-proposal.md` §9「Designer 裁定原文存档」** | **是** | **provenance + exact target + 逐字引用**，并额外声明「该评论未采用附录 B.3 的严格标记语法，标记合规性留给 integrator 判定」 |
| 3 | 提案内 §11 记录 | `docs/bcr-family-disposition` 分支，commit `16aa617`（2026-09-19 18:58:13，"record BCR background-dependence designer disposition (§11)"） | 否（未合并） | 记录 `5717715062` 一族 |
| 4 | 提案内 §8 记录 | `bcr-multitab-observation-defect-proposal-v0.md` §8（分支 `futou-/sharp-leavitt-798409`，commit `cea4071`，2026-09-19 18:59:00） | 否（未合并） | **逐字转录 `5717432171`**；对 `5718137417` 作「家族记录（含 Q→M 映射表）」、对 `5717715062` 作「姊妹裁定（另一份文档的 §11）」——即**后两条是被指向，不是被它记录** |

**准确的命题因此是**：

> 裁定记录**已经在做**，而且 `carrier-summon-v2-proposal.md` §9 那份**在默认分支上、形态相当完整**。
> 缺的是**统一形态、编号、以及入口**——同一族的裁定已散落在**两个未合并分支的两份文档的 §8/§11 里**，
> 且在文档之间**互相指向**，**没有任何编号使其可被检索**。

**这个更正使本提案的论点更强，而不是更弱**：连「逐字 + provenance + exact target」这种完整形态都已经
自发出现过（还得体地处理了「标记不合规」这一边界），说明**实践已经足够成熟，缺的只是把它固定下来**。
本提案的增量正是：**统一形态、加编号、纳入入口检索**。

**三轮更正本身就是本提案要解决的问题的最好样本**：每一版都由复审抓出前一版的不准确，
而第三版**为修第二版又漏了一处**。这与 §1.1「先看已有的」是同一条纪律——
**记录在此，因为一份关于「权威与落点」的文档，自己就演示了「落点散落、无人索引」会怎样。**

### 1.3 本提案的性质与上位依据

**上位依据（依复审意见改为论证，不再是断言）**

初稿称「规范 §6 已确立零竞争权威原则，只需下沉一级」。**这个转述不准确**，须更正：

- 规范 **§6** 的原文语境是**三层结构**（canonical protocol / 平台 bootstrap / 各仓库 `AGENTS.md`），
  其「平台层与仓库层不得自行演化为第二真源」指的是**相对规范本体**，**不是**「仓库内哪份 docs 是某概念的权威」。
  把它直接读成后者，是**断言而非论证**。

**更准确、也更有力的上位依据是规范 §1.4**——它**已经定义了一个权威文档类别**：

> 触及 **Authority 文件（指各仓库声明为权威基线 / 契约的文档，含本规范自身与 SHA 锚定的契约文件）**
> 的改动，即使位于 `docs/` 下，同样按 1.2 全项，**不得以文档路径降级**；其余纯文档可降级为引用核对。

**即：规范早就承认「权威文件」是一类需要更高审核深度的文档——缺的只是「如何声明自己是那一类」。**
本提案补的正是这个声明形式。§6 的精神（不制造第二真源）作为**类推适用**：

> **§6 的原则在协议层已确立；本提案主张其精神可类推至仓库内的文档层。** 这是本提案的**主张**，
> 需经裁定；未经裁定前不据此实施。

**由此产生的一条自我约束（来自复审 M5）**：

> 本提案中任何**触及规范 §1 / §2 的要求**（例如给 §1.3 加一项 PR body 要求），
> **只能作为「提议」提出，且必须由规范侧落地**。若在仓库层自行增设与规范并行的门禁，
> 那正是本提案所要防止的「第二真源」。§11 集中列出这类提议。

**新增概念计数（依复审 M3 更正为 2）**

初稿把「一概念一语义源」归为「规则」以把新增概念压到 1 个——**复审指出这个归类没有判据，是在回避准入门**。
采纳该意见：**按参考模型的口径，`Semantic Owner` 属于其八类**概念**之一。故本提案的新增概念为 **2 个**
（反设计登记、一概念一语义源），另加 1 项形式化（权威声明头）、1 项索引（Router）、1 项落点（裁决登记）。
两者均在 §3 逐条过准入门。

## 2. 与既有机制的对齐

| 既有机制 | 本提案如何处理 |
| --- | --- |
| 六份文档的 `SUPERSEDED — do not use as current design input` | **采纳为规范形态**；既有文档**无需重写**（§6 grandfather） |
| v3 的 `provenance tombstone` 用词 | **采纳**为该状态的正式名 |
| `v1-adjudication-record.md` | **取其结构**为裁决登记蓝本，**并补上它缺的 §2.3 字段**（该记录正文是转述，**无逐字引用块**，本身不满足 §2.3 —— 见第一轮 m3） |
| 规范 §2.1 不删除条款 | 作为退休规则依据——**注意：原文只针对「proposal 文档」**，本提案对其作泛化推广（§7 已标注） |
| 规范 §2.3 裁定记录格式 | 裁决登记的条目格式 |
| 规范 §1.4 的「Authority 文件」 | **本提案的正面依据**：为该既有类别提供声明形式 |
| 规范 §6 的「不得演化为第二真源」 | **类推适用**（§1.3 已标明这是主张而非既有条文） |
| `evidence/*.json` | 对应 `Frozen` |

## 3. 最小集设计

### 3.1 权威声明头

**消除的缺口**：缺口 1（没有 Current 的正面声明）。

**一个必须先解决的撞车（来自第一轮 M6 的延伸）**：
`Authority:` **在本仓已被使用**——`implementation-slice-1-submission-ledger-report.md` 第 3 行是
``> Authority: `futouyiba/noos_docs@a49303ca…` (Issue #5 inputs; ...)``，
含义是「本文锚定在哪个权威基线上」，即**来源指针**，**不是层级声明**。

若本提案占用同一字段名，就会让一个已在用的字段产生两种含义。**故本提案改用 `Authority-Level:`**
（`Authority:` 的既有含义保持不变，是否另行规范化为 `Baseline:` 属独立议题，本提案不主张）。

**形态**：

```markdown
> Authority-Level: Current
> Owns: HandoffPacket, HandoffResult, EscalationPolicy
> Supersedes: cross-agent-handoff-escalation-contract-v2.md@<sha>, ...
```

**取值（四个，冻结）**：

| 值 | 含义 | 作权威读？ | 与 §1.4 的关系 |
| --- | --- | --- | --- |
| `Current` | 该概念的权威读取路径 | 是 | **属 §1.4 的「Authority 文件」→ 改动走 1.2 全项** |
| `Tombstone` | 语义已吸收，仅作追溯 | 否 | 仍属契约文件 → 同样走 1.2 全项 |
| `Candidate` | 非权威；须经裁定才晋升 | 否 | 提案类，按 §1.4 该档判定 |
| `Frozen` | 可冻结数值/证据，**永不成 Current** | 否 | 证据，非契约 |

（**待裁定**：四个值是否应直接映射为 §1.4 的审核深度档？见附录 B 第 5 问。）

**纪律（承接参考模型）**：**Artifact Type ≠ Authority Level**。
文档名里的 `Final` / `Specification` / `Architecture` / `Consolidated` 只描述**用途与阅读视角**，
**不自动形成权威层级**。更详细、更新、挂得更深，都不是覆盖 Current 的理由。

### 3.2 一概念一语义源（新增概念 #2）

**消除的缺口**：缺口 2。**准入门**：它消除的是「两份 Current 都声称权威时无从裁决」这一具体失败——
不解决它，缺口 1 补上的声明反而会**制造**冲突（两份都写 `Current` 时局面比现在更糟）。

**规则**：一个概念只有一个语义源，由 `Owns:` 声明。

**可检出性**（这一条是它优于一句原则的地方）：同一概念被两份 `Current` 声明 `Owns:` ＝ **可机械检出的冲突**。
检出时：

- Router 已指明归属 → 以 Router 指向者为准；
- Router 未列或指向不明 → **视为治理缺陷**，须补 Router，**不得让 agent 自行判断**。

### 3.3 Router

**消除的缺口**：缺口 3。

**落点**：`docs/README.md`（`docs/` 目前**无 README**；单一入口覆盖两个簇）。

**形态**：「问题 → 读哪份 → 有无下钻 → 停止条件」表。

**硬规则**：Router **不存领域结论**；**「链接存在」本身不是读取理由**——上层不复制下钻内容，
但必须让 agent 知道**自己是否缺了下钻内容**。

### 3.4 裁决登记

**消除的缺口**：附带缺口（裁定记录**无统一形态、无编号、不可被入口检索**——而非「没有记录」）。

**性质**：给规范 §2.3 的要求一个 durable 落点；形态取 `v1-adjudication-record.md` 的结构 +
**补上该文件缺失的 §2.3 逐字引用字段**。

**落点**：`docs/rulings/D-YYYYMMDD-NNN.md`，**一裁定一文件**。

**为什么一文件而非单一 `DECISIONS.md`**：
（1）单文件是**合并冲突热点**（多会话并发写必撞）；
（2）单文件**无法按 exact ref 引用单条裁定**，而规范 §6 要求「裁定与合同类文件始终 SHA 锚定」。

**条目格式**（照 §2.3）：

```markdown
# D-20260919-001

- Status: in-force | pending-incorporation | superseded-by: D-xxx
- 交付来源：<谁、在何处交付>
- Exact target：`<path>@<sha>`
- 来源评论：<link>
- Provenance：<如 des: via connector>
- 决定性表述（原文引用，逐字；禁止转述改义）：
  > ...
- 约束对象：<它约束哪份 Current 的哪一段>
- Reopen gate：<什么条件下应重新考虑；无则写「不可重开」>
```

### 3.5 反设计登记（新增概念 #1）

**消除的缺口**：缺口 4（负面知识**有零散实例、无统一归属**——与附带缺口同形）。
**准入门**：它消除的是「同一类坑被重复踩、同一类方案被反复提出并被反复否决」——
注意**不是**「从无到有」（该知识已在多处文档中以不同命名存在，见 §1.2 缺口 4 的举例表），
而是**给已有的零散形态一个可检索的归口**。
判据见下（参考文献的原句），可操作、可判真假。

**落点**：`docs/pitfalls/P-YYYYMMDD-NNN.md`，一文件一条——**与裁决登记同构**（两套登记共用一种模式，刻意为之）。

**条目格式**：

```markdown
# P-20260919-001 · <一句话失败签名>

- **失败签名**（症状）：<什么现象说明你正踩这个坑>
- **为什么当时合理**：<当时的选择在当时的约束下为什么是对的>
- **Reopen gate**：<什么条件变化后应重新考虑；无则写「永久排除」>
- **来源**：<评论 / PR / 裁定 link>
```

**收录判据**（引自参考模型；**其原始出处不在本仓、我也无法直接访问，故按转述引用**）：

> 未来一个聪明但不了解今天讨论的人，是否很可能再次问「**为什么不选另一个看起来也合理的方案？**」

答「是」则收录。「为什么当时合理」必填的理由同原文：**只有这样，未来的人才知道它是永远错误，
还是在旧约束下被淘汰。**

### 3.6 裁定入库的写入路径（人类点名必答）

**（1）触发时机与责任人——本版重写（复审 BLOCKER B1）**

> **初稿错误**：初稿称「实施某裁定的 PR 无 `D-` 条目 → 不满足 §1.3 → 不得合并」，并称「门禁是机械的」。
> **复审指出该推导不成立**：规范 §1.3 只要求「PR 描述引用 **review 证据链接**与被审 **exact head** SHA」，
> 附录 A 的 CI 形态也只是「review 链接 + 7–40 位十六进制 SHA」。
> **一条 `D-` 编号既不是 review 链接也不是 SHA 形态，无法从 §1.3 推导出来。**
> 更要紧的是：若在仓库层自行增设这条门禁，**就是在做本提案所要防止的事**（制造第二真源）。

**修正后的表述**：

> **本提案提议给规范 §1.3 增加一项 PR body 要求**——实施某裁定的 PR，其 body 须引用该裁定的 `D-` 编号。
> **该修改属规范级，须按规范第 1 节走独立审核后由规范侧落地**（见 §11）。**在规范侧落地之前，
> 本项不具备强制力**，`rulings/` 的写入依赖下述责任约定。

**在规范侧落地前，责任人按约定确定**：由**执行该裁定的实现方**在其实施 PR 中固化；
若无人执行该裁定（`#57`/`#58` 正是此形态），则由 **watcher 上报**（其「未分类必上报」已覆盖 designer 原生裁定），
由人类或 orchestrator 决定是否立任务。**上报不等于归档，但至少不再静默。**

**（2）源与保真**

以**评论正文逐字照录**；规范 §2.3 禁止转述改义或代拟 designer 结论。必记：`source:`（URL + comment id）、
`exact target:`（裁定锚定的 SHA；designer 裁定自带，如 `5711118315` 锚 `bd3c7af`）、`provenance:`。

**（3）在效状态与取代链**

`Status: in-force | pending-incorporation | superseded-by: D-xxx`。**被取代时只标注、不改写正文**。

**与文档 `Authority-Level:` 是两个不同的轴**，不可混用：

| 轴 | 取值 | 回答的问题 |
| --- | --- | --- |
| 文档 | `Current` / `Tombstone` / `Candidate` / `Frozen` | 这份**文档**现在算不算权威 |
| 裁定条目 | `in-force` / `pending-incorporation` / `superseded-by` | 这条**裁定**现在还有效吗 |

一条裁定可以「作为事件已成为历史」，而其**内容**仍活在某个 `Current` 里。
**分开两个轴，正是为了避免用 `Tombstone` 去描述一条仍在生效的裁定。**

（例：`#67` 的 disposition 至今有效，宜作 `in-force` 轴的示例；**注**：它是 `PARTIAL_ACCEPT` 且**附 Required delta**，
严格说其要求尚未满足——故它同时是「in-force 但未完成」的样本，不是「已落地」的样本。）

**（4）与 Current 的双向绑定（最关键的一条）**

**裁定记录 ≠ 裁定内容。** 只有记录、没有纳入，就只是「被存档」而不是「被纳入」。

```
裁定评论（GitHub，权威源）
   │
   ├─→ 实质内容 ──→ 落到相应 Current …… 这才让裁定「生效」
   │                    └─ 该 Current 反向指回 D-xxx（可追溯）
   └─→ 事件记录 ──→ rulings/D-YYYYMMDD-NNN.md（逐字存档）
```

**硬规则**：

> 若一条裁定的内容需要改变某个 `Current` 的语义，则在该 `Current` 更新之前，
> 该条目的 `Status` **必须**标为 `pending-incorporation`，且**不得被当作已生效依据引用**。

**措辞更正（第一轮 m4）**：初稿称这使缺口「可**检出**」。实际上「该裁定是否需要改 Current」
「Current 是否已反映该裁定」都是**语义判断，无机械检出路径**。故降级为：
**有名字、可审计**——它的价值在于让缺口有一个**必须被填写的字段**，而不是让机器发现它。

**（5）与既有条款的关系**

规范 **§2.1**（**注意：不是 `AGENTS.md` §2.3——该出处确为规范 §2.1，`AGENTS.md` 无编号小节；
派单中引作出处有误，此项经复审独立确认为正确勘误**）规定 proposal 不得删除。裁决登记与之对齐：
退休只能是标注，不得删除。

**（6）谁有权写——本版重写（第一轮 M4）**

> **初稿错误**：初稿称「固化裁定＝机械照录，**不额外加独立复审**」。
> **复审指出这与规范 §1.4 冲突**：§1.4 规定触及 **Authority 文件**的改动「即使位于 `docs/` 下，
> 同样按 1.2 全项，**不得以文档路径降级**」；而 §3.6(4) 自己说条目要写「约束对象：它约束哪份 Current 的哪一段」
> ——即 `rulings/` 是**约束权威文件的文件**。
> 且 §1.5「实现者自身的测试结果不构成独立审核结论」意味着「机械照录」仍需独立核对，
> 以防 §2.3 所禁的**转述改义**。

**修正后的分级**：

| 动作 | 审核深度 |
| --- | --- |
| 固化裁定（新建 `rulings/D-*.md`，纯逐字转录） | 按 §1.4 可降级为**引用核对**——但**仍须一次独立 APPROVE**，核对逐字保真。**此项是本表中唯一的降级主张，已列入 §11 待规范侧确认** |
| 修改 `Current` 段落（产生或改变语义） | **按 1.2 全项**，不得降级 |
| 加 `Authority-Level:` 头（纯增行） | 按 §1.4，触及 Authority 文件 → **走 1.2 全项，不得降级** |

> **第三轮复审指出的更正（⑤）**：本表第 3 行原先写「确需降级须 reviewer/integrator 改判」，**该通道在规范里没有依据**——
> §1.4 规定触及 Authority 文件的改动「**不得以文档路径降级**」，而它的改判句只允许 reviewer/integrator
> **改判升格**，未给降级。且该条降级既未列入 §11，也与 §6 末段、§10 第 5 条的「加头仍需 1.2 全项」冲突。
> **已删除该降级表述**。现本表只有一处降级主张（第 1 行），且已列 §11。
> ——这与本节自己的主题直接相关：**一份主张「不得自设与规范并行的门禁」的文档，不应同时自设一条规范里没有的降级通道。**

**这条修正本身是本提案应有的样子**：初稿为了让「记录」便宜而擅自降级审核，恰好违反了
**它引用的那条规范**。复审抓住它是对的。

## 4. 拓扑

```
docs/README.md ………………………… Router（只说去哪读，不存结论）
  │
  ├─→ *-contract-*.md ……………… Current（`Authority-Level: Current` + `Owns:`）
  │     └─ Supersedes → v0/v1/v2 等 … Tombstone（既有 SUPERSEDED 措辞，grandfathered）
  │
  ├─→ rulings/D-*.md ……………… 裁定逐字存档（§2.3 记录的落点）
  ├─→ pitfalls/P-*.md …………… 反设计登记
  ├─→ evidence/*.json …………… Frozen（可冻结数值，永不成 Current）
  └─→ candidates/ …………………… Candidate
```

> **修正（第一轮 M2）**：**`candidates/` 在两个仓库的默认分支上都不存在**。
> noos-shuttle 无此目录；noos_docs 只有 `review-candidate-v1..v4.md` 与
> `v1-primary-design-sedimentation-candidate.md` 等**文件**，无 `candidates/` 目录。
> （`chatgpt-provider-recovery-go-n-proposal-v0.md` 所引的 `docs/deliberation-harness/candidates/2026-09-17-*.md`
> 两个路径在默认分支上均不存在——它们只存在于未合并分支。）
> 因此 **`candidates/` 是本提案提议的**新建**结构，不是「既有」**；它须在附录 A 过准入门。

## 5. 阅读预算与下钻提示

```
Router → 1 份 Current →（仅当需要依据时）1 份 ruling/pitfall → Stop
```

**最多 3 篇。** 超出通常意味着 Router 写得不清楚，或该问题该被拆开。

## 6. Non-retroactive（强条）

> **历史文档不追溯服从后来新增的 schema。不得为「格式统一」回写既有文档。**

- **不重写**任何既有文档的内容。
- **Grandfather 条款**：六份已用 `SUPERSEDED — do not use as current design input` 的文档，
  视为已满足 `Authority-Level: Tombstone`，**无需改写**；该旧写法**对新文档关闭**。
- 头与登记条目**按需增量**添加（该文档下次因实质原因被改动时）。
- Router **一次性编写**覆盖既有集合。
- 唯一允许的批量动作是**加头**（纯增行、不动正文），须独立成 PR。**但注意**：按 §1.4，
  加头触及 Authority 文件，仍需 1.2 全项——**「批量」不等于「降级」**。

## 7. 晋升与退休

| 从 | 到 | 条件 |
| --- | --- | --- |
| `Candidate` | `Current` | 须引用一份 `D-` 裁定 |
| `Current` | `Tombstone` | 被新的 `Current` 完整吸收时；文件保留，头改 `Tombstone`，`Supersedes` 反向指回 |
| 任何 | **删除** | 仅限规范 §2.1 允许的路径：先以 commit SHA 固化引用 |

> **范围声明（第一轮 m2）**：规范 §2.1 的不可删除条款**原文只针对「proposal 文档」**。
> 本表把它推广到**所有**文档，**这是本提案的推广，不是法条原义**。该推广须经裁定。

## 8. 验证用例走查

### 8.1 v3 的权威地位靠六份文档「指认」，自己也写了一句但没有字段（**本版替换，第一轮 m5**）

> 初稿用 `noos_docs` 的两份 Candidate 作例——但本提案范围是**本仓库 `docs/`**，
> 而 `docs/README.md` 这个 Router **不可能覆盖另一个仓库**。该用例在自身范围内走不通。

**改用范围内对象**：本仓库的 `cross-agent-handoff-escalation-contract-v3.md` 自称
"Consolidated Current Design Contract"，而**六份文档都指向它**——但那句是**散文而非字段**：
它无法被机械读取，因此入口（Router）也无从据此生成，只能靠人把这句话抄进去。
新规则下它写 `Authority-Level: Current` + `Owns: <契约概念>`，六份指向它的事实就由 Router 集中登记。
→ **从任何一份进去都能立刻知道谁是权威**，而不是像现在只能读到「我不是」。

### 8.2 `#57` / `#58` 裁定无统一形态与编号（**本版按复审两轮意见再次更正**）

**准确现状**（依 §1.2 的实测表）：裁定记录**已经在做**，且 `docs/carrier-summon-v2-proposal.md` §9 那份
**已在默认分支上、形态完整（provenance + exact target + 逐字引用）**；
`#57` 分支的提案另由 `cea4071`（2026-09-19 18:59）在 §8 内记录了同一族裁定。
**缺的是统一形态与编号**，不是记录本身。

**新规则下**：上述形态被统一为 `docs/rulings/D-<...>.md`（§2.3 格式、带编号、可被 Router 检索到），
提案正文引用该编号；且因内容需改 `Current`，条目须标 `pending-incorporation` 直至 Current 更新。

**这个用例的意义变了三次，最终停在更准确也更难反驳的位置**：它证明的不是「没有落脚点」，
而是**已有落脚点各自为政、不可检索**——本提案把它们标准化。

### 8.3 v0–v3 版本并存

**已解决一半**：六份头部都写了 `SUPERSEDED` 并指向 v3。
**仍缺**：那个「指向 v3」是**手写散文、无字段**（且六份第三行措辞有微差）；v3 无正面声明；
无处集中列出「有哪些 Current」。新规则下补 `Authority-Level: Current` + Router，**两侧都成立**。

### 8.4 本提案自身（自指检验）

**本版已补上头部**（见文首）：`Authority-Level: Candidate` + `Owns: DocGovernance`。
裁定通过后依 §7 晋升为 `Current`，并引用该裁定的 `D-` 编号。

> **第一轮 m6 已采纳**：初稿只在 §8.4 里**声称**应有该头，而**文件本身没有**，属纸面推演。
> 本版实际写入，自指检验由「论证」变为「演示」。

## 9. 实施切片

1. **Router**：写 `docs/README.md`
2. **两套登记的模板 + 首批补录**：模板 + 把 `#57`/`#58` 的裁定补录为首批 `D-`
3. **补 `Current` 正面声明**：先补 v3 等少数真正的权威文档（缺口 1 的最小动作）
4. **`docs/` 根产品文档按需标注**（增量，不批量）
5. **（可选）可检验性脚本**：**建议先不做**（见 §10 第 1 条）

## 10. 未决与风险

1. **过度治理（本提案最大风险）**。参考模型自带 `Governance Freeze`；本提案新增 **2 个概念**
   （反设计登记、一概念一语义源），另有 1 项形式化（权威声明头）+ 1 项索引（Router）+ 1 项落点（裁决登记）。
   若裁定过程中再加，应逐条过准入门。**§9 切片 5 建议先不做**：它会把治理再扩一层，而当前冲突量不需要机器检。
2. **Router 粒度**未定：单入口是否够？建议先用单入口。
3. **覆盖范围**：本提案覆盖 `docs/`。`.noos/` 下文档不在范围内。
4. **「一个概念」的边界**：只在出现实际冲突时才去界定，不预先给清单。
5. **Non-retroactive 与批量加头的张力**：边界是「加头可、改正文不可」；但按 §1.4 加头仍需全项审核（§6 末段）。
6. **Grandfather 的代价**：旧散文 / 新字段两种写法并存，校验要认两种。
7. **`Tombstone` vs `Historical` 的用词争议——待人类裁定（占位）**。
   本提案用既有词 `Tombstone`；反对理由是「生命周期极性不同」并举 `#67` 为反例。
   §3.6(3) 已给出轴分离的解释（文档权威 vs 裁定在效是两个轴）。**用词归人类定**，裁定前不据此实施。
8. **`candidates/` 的原状**（复审 M2）：本提案把它列为**待新建**，但参考模型把 `Candidate` 当作既有类。
   需一并裁定：是新建目录，还是沿用 noos_docs 的 `*-candidate.md` 命名（后者已有实例）。

## 11. 本提案需要规范侧改动的地方（新增，来自复审 M5）

以下**只能提议、必须由规范侧落地**。在落地前，本提案相关条款**不具备强制力**：

| 提议 | 触及 | 理由 |
| --- | --- | --- |
| §3.6(1)：PR body 须引用实施裁定的 `D-` 编号 | 规范 §1.3 | 这是新的合并门禁要求。**在仓库层自行增设它就是制造第二真源** |
| §7 的不可删除推广到所有文档 | 规范 §2.1 | 原文只针对 proposal 文档 |
| §3.1 的取值与 §1.4 审核深度档的映射 | 规范 §1.4 | 若映射，则属对该节的具体化 |
| §3.6(6) 对「固化裁定」审核深度的重新指派（可降级为引用核对） | 规范 §1.4 | 该节规定触及 Authority 文件的改动不得以文档路径降级；本条**主张** `rulings/` 的逐字转录属可降级情形。**这是对 §1.4 的具体化主张，须由规范侧确认**；确认前按 §1.4 从严执行 |

## 附录 A：与参考模型的差异（哪些没采纳、为什么）

参考模型列了**八类概念**，本提案**不照搬**：

| 参考模型 | 是否采纳 | 理由 |
| --- | --- | --- |
| Router | **采纳** | 对应缺口 3 |
| Semantic Owner（一概念一语义源） | **采纳（计为新增概念）** | 缺口 2 的正解；**本版已按复审 M3 更正归类** |
| Historical | **采纳但用既有词 `Tombstone`** | 避免同一概念两个名字 |
| Validation-Frozen | **采纳** | 对应既有 `evidence/` |
| Candidate | **采纳，但容器待定** | **默认分支无 `candidates/`**（复审 M2）；见 §10 第 8 条 |
| Pitfall Registry | **采纳** | 对应缺口 4 |
| Project / Domain Current 两级分层 | **未采纳** | 为「跨域工作区」设计；本仓库单一产品线，采用会违反准入门 |
| Current Fact Owner | **未采纳** | 其存在理由是「同一数值多处消费需单一真源」，本仓无此形态 |
| Consumer（读但不获重新定义权） | **未采纳** | 那是**权限**概念；本仓等价物是 review 门禁与写权限 |
| 7 项完整性 × 2+N 知识包 | **未采纳** | 那是**写作标准**，不是**治理结构** |

## 附录 B：请 Epic Designer 裁定

1. **§1.3 的上位依据是否成立？** 即：以规范 §1.4 已有的「Authority 文件」为正面依据、
   并把 §6 的精神**类推**至仓库文档层——这个论证是否成立。
2. **新增的 2 个概念**（反设计登记、一概念一语义源）是否通过准入门？
   （另 3 项非概念：权威声明头＝形式化既有、Router＝索引、裁决登记＝§2.3 记录的落点。）
3. **权威头采纳既有 `SUPERSEDED` 措辞 + grandfather 条款**（旧写法不再用于新文档、但不回写），是否认可？
4. **裁决登记用「一裁定一文件」而非单一 `DECISIONS.md`**，是否认可？
5. **`Authority-Level:` 的四个取值是否合适？是否应直接映射 §1.4 的审核深度档？**
   （本提案已因撞车放弃 `Authority:` 字段名——`Authority:` 在本仓已用作来源指针。）
6. **`Consolidated` 这类前缀**是否进入取值域？（按 `Artifact Type ≠ Authority Level`，倾向否）
7. **§3.6 的写入路径是否成立？** 特别是：
   （a）它**依赖规范 §1.3 的修改**（§11），在此之前无强制力——这个诚实标注是否可接受；
   （b）`pending-incorporation` 状态（内容未落 Current 前不得被当已生效依据引用）。
8. **写权分级**（§3.6(6) 修正版）：固化裁定可降级为「引用核对但**仍须一次独立 APPROVE**」，
   改 Current 走 1.2 全项——是否认可？
9. **§7 的推广**（不可删除扩展到所有文档）与 **§10 第 8 条**（`candidates/` 新建还是沿用命名）如何定？

**另一项由人类单独裁定**：`Tombstone` vs `Historical` 用词（§10 第 7 条）。不影响其余条款。

## 附录 C：本版相对初稿的修订记录（回应复审 `REQUEST_CHANGES`）

> **标签约定（第三轮复审 ④ 指出）**：正文中的引用标签**按轮次区分**，第一轮与第二轮各有一套，且**有重名**：
>
> - **第一轮**（`5741397820`）：`B1` / `M1–M6` / `m1–m7` / `n1–n4`
> - **第二轮**（`5741444110`）：`MAJOR-1` / `MINOR-1..3` / `NIT-1..3`
> - **第三轮**（`5741495231`）：`①–⑤`（非阻塞项）
> - **第四轮**（`5741542439`）：`①–⑤` 的更正（约定块项数写错 与 ④ 的残留标签）
> - **第五轮**（`5741584950`）：本约定块的「一律/全部」与「两张表」两处不实断言
> - **第六轮**（`5741620094`）：表数计数与 Rejected 章节计数两处被证伪
>
> 因此「**第一轮 M5**」与「**第二轮 NIT-1**」**不是同一条**。
> 两类编号**只有一处会撞名**：第一轮的 `mN` 与第二轮的 `MINOR-N` 形态相近。
> 故约定只有一条：**引用第一轮的 MINOR 条目时必须带「第一轮」前缀**，不得写成裸的 `MINOR-N`。
> 其余第一轮标签（`B1`、`MN`、`nN`）在第二轮的集合里没有对应物，**裸写不产生歧义，故正文中保留**。
> 附录 C 的表**按轮次分节**，表内标签只在本表内有意义。
>
> **此处刻意不写表的张数。** 本句原先写过「两张」→「三张」，两次都不对（第二次是因为**同一笔编辑
> 又新增了两节**，而计数是按编辑前的文件算的）。**这是全文关于自身结构的最后一个计数，已删除**——
> 见下方「第六轮」节的「关于自指计数」。

| 复审 finding | 处置 |
| --- | --- |
| **B1**（§3.6(1) 门禁推导不成立） | **重写** §3.6(1)；删去不成立的推导与「门禁是机械的」；改为「提议修改规范 §1.3」，并新增 **§11** 集中列出需规范侧落地的项 |
| **M1**（「命中 0」与分支现状矛盾） | **收窄**为「默认分支无裁定条目」；承认 `cea4071`（18:59，早于本文 13 分钟）已在 §8 内记录；并据此把本提案的增量重述为「标准化已有落脚点」 |
| **M2**（`candidates/` 不存在） | **已核并更正**：两个仓库默认分支均无该目录；§4 加注、附录 A 改为「容器待定」、§10 加第 8 条 |
| **M3**（概念/规则归类回避准入门） | **采纳**：新增概念计数更正为 **2**，两者均过准入门 |
| **M4**（写权分级与 §1.4 冲突） | **重写** §3.6(6)：固化仍需一次独立 APPROVE（引用核对），改 Current 走 1.2 全项 |
| **M5**（§6 转述不准确 + 自相矛盾之嫌） | **重写** §1.3：§6 改为「精神类推」的**主张**；以 §1.4 为正面依据；新增 **§11** 承接受规范侧落地的项 |
| **M6**（8 份计数含假阳性） | **更正为 7**，并说明该假阳性的来源 |
| m1（口径未注明） | §1.2 缺口 3 已注明「含本文 / 不含本文」两个口径 |
| m2（§2.1 推广未声明） | §7 加范围声明 |
| m3（蓝本缺项） | §2 表与 §3.4 已注明该记录正文是转述、缺 §2.3 逐字字段 |
| m4（「可检出」过强） | §3.6(4) 降级为「有名字、可**审计**」 |
| m5（§8.1 超出范围） | §8.1 **替换**为本仓内的 v3 用例 |
| m6（自指检验是纸面） | **已在文首实际写入** `Authority-Level: Candidate` + `Owns:` |
| m7（准入门未给原文 + 内部交叉引用错误） | §3.5 已注明判据系转述引用；「§1.3 那道准入门」的错误引用已改为准确表述 |
| n1–n4 | 已分别处理：第三行措辞微差（§1.1）、v3 的 `Status:` 并存（附录 B 第 6 问）、Status 取值集一致（§3.4 与 §3.6(3)）、`#67` 附 Required delta 的注明（§3.6(3)） |

### 第二轮（增量复审 @ `5c2f935`）

前轮 17 条中，B1 / M2–M6 / m1–m7 / n1 / n3 / n4 经复审核验为**真正处置**（非仅声称），n2 部分处置。
但本轮为修 M1 而**新引入**了若干不一致，故第二次 `REQUEST_CHANGES`。

| 第二轮 finding | 处置 |
| --- | --- |
| **MAJOR-1**：§1.2 / §8.2 的**全域否定不成立**——「默认分支无任何裁定条目」与本文 §1.1 自己列出的既有证据矛盾；且 `carrier-summon-v2-proposal.md` §9 已把 designer 裁定**逐字**存档在 `main` 上 | **重写 §1.2 附带缺口为「三种既有形态」实测表**，并新增 `carrier-summon-v2-proposal.md` §9 这一形态；§8.2 同步更正。**该更正使本提案论点更强**：连「逐字 + provenance + exact target」的完整形态都已自发出现 |
| **MINOR-1**：新增概念/规则计数三处不一致（§1.3 与 §10.1、附录 B 第 2 问互斥） | 三处统一为「2 个概念 + 1 项形式化 + 1 项索引 + 1 项落点」 |
| **MINOR-2**：§1.2 出现新的悬空内部引用（指向已改写的旧 §1.3 标题） | 该句已随 §1.2 重写删除 |
| **MINOR-3**：§11 漏列 §3.6(6) 对 §1.4 审核深度的重新指派 | §11 已补该行，并注明「确认前按 §1.4 从严执行」 |
| **NIT-1**：`13,143` 非任何口径的真实值 | **直接去掉该自指数字**——本文不再引用包含自身的行数（任何编辑都会使其过期，本项两轮错了两次）。现只保留不含本文的稳定口径：61 份 `.md` / 12,640 行 / 65 文件 / 23,152 行 |
| **NIT-2**：§8.1 标题仍指向被替换的对象 | 标题已改为「v3 的权威地位靠六份文档『指认』而无正面声明」 |
| **NIT-3**：§1.1 的引文不逐字节（漏反引号与括号） | 已补为 ``> Current candidate: `cross-agent-handoff-escalation-contract-v3.md` (consolidated current contract)``。**第三轮**指出：单反引号码段内用「反斜杠+反引号」转义**不生效**（GFM 码段不处理转义），会渲染错误 → 已改用**双反引号**定界（§1.1、§3.1 两处同样处理） |

### 第三轮（增量复审 @ `d1a2e2c`）—— **APPROVE**

复审逐格核验了§1.2 的「既有形态」表每一格（含 `git ls-tree origin/main`、`git show origin/main:docs/carrier-summon-v2-proposal.md`
逐段比对、`git branch --contains cea4071`）、全文一致性（编号连续、取值域一致、规范交叉引用逐条取回核对），
**确认 MAJOR-1 的实质已修好、无残留互斥**，并明确「其余问题全是排版/口径类，**无一致使实质主张为假**」。
它的 APPROVE **仅覆盖本轮增量**，不构成对正文设计（§3–§7）的背书。

第三轮留下的 5 条非阻塞项，本版一并处理：

| 第三轮项 | 处置 |
| --- | --- |
| **①**（本轮新引入）§1.2 口径注写「以上**三个**数字」而列了四个 | 已改为「以上**四个**数字」——出现在「我已不再引用自指数字」的声明句里，属同类失败的余响 |
| **②**（本轮新引入）单反引号码段内用「反斜杠+反引号」转义**不生效**（GFM 码段不处理转义），引文渲染错误，反而多出两个反斜杠 | 已改用**双反引号**定界（§1.1、§3.1、附录 C 三处） |
| **③**（本轮新引入的措辞）归因过头——cea4071 §8 **逐字转录的是 `5717432171`**，`5718137417`/`5717715062` 是**被指向**；且**另有第四处**（分支 `docs/bcr-family-disposition`，commit `16aa617`，2026-09-19 18:58:13） | §1.2 表扩为**四种位置**并逐格订正完整度；归因句改为「散落在**两个未合并分支的两份文档的 §8/§11 里**，且互相指向」 |
| **④**（既存）正文引用标签在两轮之间撞名（`NIT-1` / `MINOR-3` / `MINOR-5` 处于两轮有不同所指） | 附录 C 开头新增**标签约定**说明（按轮次区分），并在 §1.2 等处以「第一轮 m1」「第二轮 NIT-1」形式逐处标注轮次 |
| **⑤**（既存）§3.6(6) 第 3 行给出的「加头可降级」通道**在规范里没有依据**（§1.4 的改判句只允许**升格**），且未列入 §11、并与 §6 末段/§10 第 5 条冲突 | **已删除该降级表述**；现该表只有一处降级主张（固化裁定），且已列 §11。**这与本节自身主题直接相关：一份主张「不得自设与规范并行的门禁」的文档，不应同时自设一条规范里没有的降级通道** |

**复审未覆盖 / 不可核验**：参考模型本体（Notion）不可达，故其「八类概念」、准入门原文、`Governance Freeze`
只能按转述评估；② 的渲染结论系依 CommonMark/GFM 规则推导（环境无 markdown 解析器可实测）。

### 第四轮（增量复审 @ `3dad8b4`）—— `REQUEST_CHANGES`

①②③⑤ 经复审核验为**真正处置**（含 ③ 的四位置表**逐格**复核属实：`16aa617` 存在且 subject 相符、
cea4071 §8 逐字转录的确实是 `5717432171`、后两条为「被指向」）。只卡两处：

| 第四轮项 | 处置 |
| --- | --- |
| 约定块把第三轮项数写成 `①–⑦`（实为 `①–⑤`，第三轮 verdict 原文只有五个圆圈数字，且本文下一段自称「5 条」） | 改为 `①–⑤`，并补第四轮真实评论 ID。**这是「局部修补处自引入不一致」的第三次** |
| ④ 残留：正文 7 处裸标签（`MINOR-2/3/4/5/6`、`MAJOR M2/M4`）按约定查不到格或走错格 | 7 处全部改为真实所指（`第一轮 m2–m6` / `M2` / `M4`），**编号与上下文逐条相符**（第五轮复核 7/7 正确） |
| （另记录）附录 C 两处散文单反引号未闭合 | 改为描述性写法「反斜杠+反引号」 |

### 第五轮（增量复审 @ `4ccc8ee`）—— `REQUEST_CHANGES`

前轮两项最小修正经核验**全部改对**（含 7/7 编号正确、四个评论 ID 逐个验真、以及**该轮当时的**表格列数整齐）。
但**约定块自身又写了两条它违反的断言**，外加**本文自查发现的一处承重事实错误**：

| 第五轮项 | 处置 |
| --- | --- |
| 约定块写「正文引用第一轮条目时**一律**写作「第一轮 mN」」「此约定覆盖正文**全部**交叉引用」——实测正文有 7 处仍是裸形（`M5`/`M3`/`B1`/`M2`） | **收窄为准确表述**：两类编号**只有一处会撞名**（第一轮 `mN` vs 第二轮 `MINOR-N`），故约定只有一条——引用第一轮 MINOR 条目须带前缀；其余裸写无歧义、保留 |
| 约定块写「附录 C **两张表**各自带轮次标题」——实测是三张，且第一轮那张无独立轮次标题 | 第五轮改为「共三张…」——**该修正本身在第六轮被证伪**（见下），现已**删去计数** |
| **本文自查（非复审提出）**：§1.2 缺口 4 原写「负面知识**没有任何容器**」 | §1.2 缺口 4 重写为「有零散实例、无统一归属」，与附带缺口**同形**；§0、§3.5 的准入门表述同步更正。**该更正本身在第六轮被证伪**（见下） |

**第五轮的意义**：这是**第一次由本文自查、而非复审，发现一条承重事实错误**——
触发它的是对**全称量词**（「任何」「一律」「全部」）的系统清剿。

### 第六轮（增量复审 @ `1bafb84`）—— `REQUEST_CHANGES`

第五轮的两项修正**均未达成**，而且都是同一根：**把可变的计数写进正文**。

| 第六轮项 | 处置 |
| --- | --- |
| 「附录 C 共**三张**表」——实测交付 head 上是 **5 张**（第二轮那张 + 第三、四、五轮各一张 + 第一轮那张）。**根因**：那个「三」是按**编辑前**的文件算的，而**同一笔编辑又新增了两节两张表** | **删去计数**（不再写表的张数），只保留「按轮次分节」 |
| 「实测 `docs/` 下已有 **2 份**文档带 `## Rejected Alternatives` 章节」——**假的**。复审验了代码围栏奇偶性：`noos-context-pack-background-layer.md` 的那处在**围栏内**，是 §5.6 的模板**建议**；另一份是小写 `## Rejected alternatives`。**真实章节只有 1 份**；而按概念算，另有 `## Why Not …`、多处 `## Non-goals`、`### 为什么不能…` 等**至少 7 处**被漏掉 | §1.2 缺口 4 改为**举例表（明示不穷举）**，不再给数字；§3.5、本条同步更正 |
| 「这类信息只存在于评论里，既不在那 2 份…文档中」——类别过宽（被点名的那份文档里就有 `Do not reopen settled direction…` 与 Reopen condition 列） | 已**窄读**：限定为「逐条点名某 PR / Issue 的 Qn」那类裁定，并显式声明不是「负面知识一律不在文档里」 |
| §8.1 正文本轮被改写但未记入附录 C；标题仍写「而无正面声明」，与新正文相互拉扯 | 标题已改为「**靠自己无字段可读的 `Status:` 散文**」；本条即其记录 |

**关于自指计数（本 PR 的最终教训）**

到此为止，本文档写过的**每一个关于自身的计数都至少过期或出错一次**：本文行数（`440`→`507`→…）、
`docs/` 文档数（`65`→`61`）、附录 C 表数（`两张`→`三张`→实为 `五张`）、
「8 份带权威头部的文档」（实为 `7`）。

**规则**：**正文中不再出现关于本文档自身结构的计数。**
关于**外部对象**的计数（`docs/` 下 61 份 `.md` / 12,640 行 / 65 文件 / 23,152 行）可以保留——
它们不含本文、不随本文编辑而变，且已各自复核。

这个规则不是洁癖：本 PR 六轮里有**四轮**卡在这上面，其中两轮是**同一句话连续错两次**。
