# trailing-punct-phrase-bonus

Query '总资产。' (with trailing 。). Page with '总资产' in BOTH title and content should still rank first and titleMatch=true, the same as if the user had typed '总资产' without the period.
