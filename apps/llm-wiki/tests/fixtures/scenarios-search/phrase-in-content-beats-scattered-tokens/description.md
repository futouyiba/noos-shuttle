# phrase-in-content-beats-scattered-tokens

Query 'self-attention'. tokens split to ['self','attention']. The page containing the literal phrase 'self-attention' should outrank the page where 'self' and 'attention' appear separately.
