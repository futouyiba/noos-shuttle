# llm-judged/semantic-match

Rule stage can't match 'Missing: Context Window' to any existing page by filename or title. The LLM, given the wiki state, returns the review ID as resolved (attention.md covers context windows). Uses a fenced JSON response to exercise extractJsonObject.
