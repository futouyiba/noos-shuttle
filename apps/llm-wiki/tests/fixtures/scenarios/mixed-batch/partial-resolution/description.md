# mixed-batch/partial-resolution

A mix of reviews: one resolvable by rules (filename match), one contradiction that must stay pending, one missing-page the LLM confirms as resolved, one suggestion the LLM conservatively keeps.
