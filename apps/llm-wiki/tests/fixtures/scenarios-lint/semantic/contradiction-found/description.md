# semantic/contradiction-found

Two cross-linked pages make conflicting claims. Structural lint sees no issues, but the mocked semantic LLM response emits a LINT block that the parser extracts into a contradiction finding.
